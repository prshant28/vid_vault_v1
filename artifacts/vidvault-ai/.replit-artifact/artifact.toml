kind = "web"
previewPath = "/"
title = "VidVault AI"
version = "1.0.0"
id = "artifacts/vidvault-ai"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 3000

[services.development]
run = "PORT=3000 BASE_PATH=/ pnpm --filter @workspace/vidvault-ai run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/vidvault-ai", "run", "build" ]
publicDir = "artifacts/vidvault-ai/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "3000"
BASE_PATH = "/"
