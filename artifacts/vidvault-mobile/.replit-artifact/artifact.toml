kind = "mobile"
previewPath = "/mobile/"
title = "VidVault Mobile"
version = "1.0.0"
id = "artifacts/vidvault-mobile"
router = "expo-domain"

[[integratedSkills]]
name = "expo"
version = "1.0.0"

[[services]]
ensurePreviewReachable = "/status"
name = "expo"
paths = [ "/mobile/" ]
localPort = 19444

[services.development]
run = "pnpm --filter @workspace/vidvault-mobile run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/vidvault-mobile", "run", "build" ]
run = [ "pnpm", "--filter", "@workspace/vidvault-mobile", "run", "serve" ]

[services.env]
PORT = "19444"
BASE_PATH = "/mobile/"
